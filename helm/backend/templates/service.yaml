apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.http.port }}
      protocol: TCP
      targetPort: {{ .Values.service.http.targetPort }}
  selector:
    app: {{ .Release.Name }}    
